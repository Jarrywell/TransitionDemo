<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.test.transition.demo"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SimpleTransitionActivity"/>
        <activity android:name=".SimpleSceneActivity"/>
        <activity android:name=".SimpleDemo1Activity"/>

        <activity android:name=".content_transition.ContentTransitionA" android:theme="@style/TestTransitionTheme"/>
        <activity android:name=".content_transition.ContentTransitionB" android:theme="@style/TestTransitionTheme"/>

        <activity android:name=".share_element_transition.ShareElementTransitionA" android:theme="@style/TestTransitionTheme"/>
    </application>

</manifest>